%This script will calculate the area of a rectangle.

length = input('Enter the length: ');
width = input('Enter the width: ');

area = length*width;
fprintf('The area of the rectangle is %.2f.\n',area)